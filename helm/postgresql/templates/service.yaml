apiVersion: v1
kind: Service
metadata:
  name: {{.Values.postgresql.service.name}}
  namespace: demo
  labels:
    app: postgresql
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.postgresql.service.port }}
      targetPort: 5432
      protocol: TCP
      name: postgresql
  selector:
    app: postgresql